rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Any signed-in user (including anonymous kiosk sessions) can read and write.
      allow read, write: if request.auth != null;
    }
  }
}
